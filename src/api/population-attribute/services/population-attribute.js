'use strict';

/**
 * population-attribute service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::population-attribute.population-attribute');