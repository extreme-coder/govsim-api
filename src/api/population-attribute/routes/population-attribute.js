'use strict';

/**
 * population-attribute router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::population-attribute.population-attribute');